[Unit]
Description=ContrastScan Security Scanner
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/contrastscan/app
ExecStart=/opt/contrastscan/venv/bin/uvicorn main:app --host 127.0.0.1 --port 8001
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
